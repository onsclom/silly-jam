import { AnyPlayer, Player } from './types';
export declare const spud: {
    /**
     * player 1
     */
    p1: {
        /**
         * "P1" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    };
    /**
     * player 2
     */
    p2: {
        /**
         * "P2" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    };
    /**
     * player 3
     */
    p3: {
        /**
         * "P3" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    };
    /**
     * player 4
     */
    p4: {
        /**
         * "P4" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    };
    /**
     * player with the most recently active gamepad.
     * before any button presses or gamepads are connected, this is `spud.p1`.
     * afterwards, this is the last gamepad with a button press, trigger value > 0.75, or normalized joystick magnitude > 0.75.
     */
    singlePlayer: {
        /**
         * "P1"-"P4" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    };
    /**
     * array of all players (p1-p4), including those without connected gamepads.
     * this array will always have a length of 4.
     */
    readonly players: readonly [{
        /**
         * "P1" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    }, {
        /**
         * "P2" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    }, {
        /**
         * "P3" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    }, {
        /**
         * "P4" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    }];
    /**
     * array of players with connected gamepads, filtered to only include the currently connected ones.
     * @example - If P1 and P3 are disconnected, the value is equal to:
     * ```
     * [spud.p2, spud.p4]
     * ```
     */
    readonly connectedPlayers: {
        /**
         * "P1"-"P4" by default, or a 1-3 character player name if player-customized on the spud.gg website
         */
        readonly name: string;
        readonly color: string;
    }[];
    /**
     * `true` if the game is running in demo mode on spud.gg.
     *
     * note: when your game is in an iframe on spud, we automatically add `?mode=demo` to the querystring
     * so that you can conditionally disable gamepad inputs and sounds, and load fewer assets for the demo.
     */
    readonly isDemoMode: boolean;
    /**
     * true if the game is currently paused
     */
    readonly isPaused: boolean;
    /**
     * pause the game.
     *
     * if in an iframe connected to the spud.gg website using the `listen()` method, this also triggers the global pause overlay and sends focus back to the parent window.
     */
    pauseGame(): void;
    /**
     * initialize the spud api and listen for "pause", "resume", and "playerinfo" postMessage events
     */
    listen(): void;
    /**
     * disconnect the spud api and remove postMessage event listeners
     */
    disconnect(): void;
    /**
     * check if the spud api is already initialized and listening for postMessage events
     */
    readonly isListening: boolean;
    /**
     * spud player colors in oklch color space corresponding with tailwind v4 colors
     * P1: sky-400, P2: rose-400, P3: yellow-400, P4: emerald-400
     */
    colors: {
        0: string;
        1: string;
        2: string;
        3: string;
    };
};
export declare function createPauseEventMessage(): {
    type: "pause";
    player: Player | AnyPlayer;
};
export declare function createResumeEventMessage(player: Player | AnyPlayer): {
    type: "resume";
    player: Player | "AnyPlayer";
};
export declare function createPlayerInfoMessage({ players, }: {
    players: [{
        name: string;
    }, {
        name: string;
    }, {
        name: string;
    }, {
        name: string;
    }];
}): {
    type: "playerinfo";
    players: [{
        name: string;
    }, {
        name: string;
    }, {
        name: string;
    }, {
        name: string;
    }];
};
