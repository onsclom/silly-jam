import { Axis, Button, Player, SpecificPlayer } from './types';
export declare const previousGamepadButtonsDown: readonly [Set<Button>, Set<Button>, Set<Button>, Set<Button>];
export declare const previousGamepadAxes: readonly [
  Map<Axis, number>,
  Map<Axis, number>,
  Map<Axis, number>,
  Map<Axis, number>,
];
/**
 * Saves the current state of all gamepads for the next frame comparison
 */
export declare function saveGamepadState(): void;
/**
 * Checks if a button is currently pressed
 */
export declare function isButtonPressed(button: Button, player?: Player): boolean;
/**
 * Checks if a button was pressed in the previous frame
 */
export declare function wasButtonPressed(button: Button, player?: Player): boolean;
/**
 * Reads the current value of an axis for a specific player
 */
export declare function readAxis(axis: Axis, player: SpecificPlayer): number | undefined;
/**
 * Detects if a button was just pressed (not pressed in previous frame, but pressed now)
 */
export declare function gamepadButtonJustPressed(button: Button, player?: Player): boolean;
/**
 * Detects if a button was just released (pressed in previous frame, but not pressed now)
 */
export declare function gamepadButtonJustReleased(button: Button, player?: Player): boolean;
/**
 * Detects if a button is being held (pressed in both previous and current frame)
 */
export declare function gamepadButtonHeld(button: Button, player?: Player): boolean;
/**
 * Detects if a gamepad was just connected
 */
export declare function gamepadJustConnected(player: SpecificPlayer): boolean;
/**
 * Detects if a gamepad was just disconnected
 */
export declare function gamepadJustDisconnected(player: SpecificPlayer): boolean;
/**
 * Detects the type of gamepad based on its ID
 */
export declare function getGamepadType(player: SpecificPlayer): 'Xbox' | 'Ps' | 'Generic';
/**
 * Returns 0 for very tiny values to prevent floating point issues
 */
export declare const zeroTinyValues: (value: number) => number;
/**
 * Normalizes an axis value taking into account the deadzone
 */
export declare function normalizeAxisValue(value: number, deadzone?: number): number;
/**
 * Snaps an analog stick position to evenly spaced radians
 */
export declare function snapToRadian(
  x: number,
  y: number,
  numberOfSnaps: number,
  deadzone?: number,
): {
  x: number;
  y: number;
};
/**
 * Snaps analog stick to 8 directions
 */
export declare function snap8(
  x: number,
  y: number,
  deadzone?: number,
): {
  x: number;
  y: number;
};
/**
 * Snaps analog stick to 4 directions
 */
export declare function snap4(
  x: number,
  y: number,
  deadzone?: number,
): {
  x: number;
  y: number;
};
/**
 * Keeps only the vertical component of the stick position
 */
export declare function vertical(
  x: number,
  y: number,
  deadzone?: number,
): {
  x: number;
  y: number;
};
/**
 * Keeps only the horizontal component of the stick position
 */
export declare function horizontal(
  x: number,
  y: number,
  deadzone?: number,
): {
  x: number;
  y: number;
};
/**
 * Helper function to check if a value exists
 */
export declare function exists<T>(value: T | null | undefined): value is T;
