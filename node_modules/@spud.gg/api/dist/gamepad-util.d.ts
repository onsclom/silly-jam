import { GamepadBaseLayout, GamepadModel } from './gamepad-ids';
import { AllPlayers, AnyPlayer, Axis, Button, CardinalDirection, Motion, Neutral, Player, Side } from './types';
export declare const previousGamepadButtonsDown: readonly [Set<Button>, Set<Button>, Set<Button>, Set<Button>];
type AxisMotionHorizontal = typeof Motion.Left | typeof Motion.Right | Neutral;
type AxisMotionVertical = typeof Motion.Up | typeof Motion.Down | Neutral;
export declare const resume: {
    time: number;
    player: Player | AnyPlayer;
};
export declare let lastActiveGamepad: Player;
/**
 * save the current state of all gamepads at the end of the frame for the next frame comparison
 */
export declare function clearInputs(): void;
/**
 * checks if a button is currently pressed
 */
export declare function isButtonPressed(button: Button, player?: Player | AnyPlayer | AllPlayers): boolean;
/**
 * checks if a button was pressed in the previous frame
 */
export declare function wasButtonPressed(button: Button, player?: Player | AnyPlayer | AllPlayers): boolean;
/**
 * `true` if a button was just pressed this frame and was not pressed last frame
 */
export declare function gamepadButtonJustPressed(button: Button, player?: Player | AnyPlayer | AllPlayers): boolean;
/**
 * `true` if a button was just released this frame and was pressed last frame
 */
export declare function gamepadButtonJustReleased(button: Button, player?: Player | AnyPlayer | AllPlayers): boolean;
/**
 * `true` if a button is pressed this frame and was pressed last frame
 */
export declare function gamepadButtonHeld(button: Button, player?: Player | AnyPlayer | AllPlayers): boolean;
export declare function gamepadJustConnected(player: Player): boolean;
export declare function gamepadJustDisconnected(player: Player): boolean;
/**
 * reads the current value of an axis (analog stick) for a specific player
 */
export declare function readAxis(axis: Axis, player: Player): number | undefined;
/**
 * get all identifying gamepad properties, including vendor and model
 */
export declare function getGamepadInfo(player: Player): {
    readonly id: string;
    readonly index: number;
    readonly mapping: GamepadMappingType;
    readonly timestamp: number;
    readonly model: GamepadModel;
    readonly vendorId: string;
    readonly productId: string;
    readonly productName: string;
    readonly baseLayout: GamepadBaseLayout;
};
export declare const defaultDeadzone = 0.25;
/**
 * returns 0 for very tiny values to prevent floating point issues
 */
export declare const zeroTinyValues: (value: number) => number;
/**
 * normalizes an axis value taking into account the deadzone
 */
export declare function normalizeAxisValue(value: number, deadzone?: number): number;
/**
 * snaps an analog stick position to evenly spaced radians
 */
export declare function snapToRadian(x: number, y: number, numberOfSnaps: number, deadzone?: number): {
    x: number;
    y: number;
};
/**
 * snaps analog stick to 8 directions
 */
export declare function snap8(x: number, y: number, deadzone?: number): {
    x: number;
    y: number;
};
/**
 * snaps analog stick to 4 directions
 */
export declare function snap4(x: number, y: number, deadzone?: number): {
    x: number;
    y: number;
};
/**
 * keeps only the vertical component of the stick position
 */
export declare function vertical(x: number, y: number, deadzone?: number): {
    x: number;
    y: number;
};
/**
 * keeps only the horizontal component of the stick position
 */
export declare function horizontal(x: number, y: number, deadzone?: number): {
    x: number;
    y: number;
};
/**
 * calculates the magnitude (length) of a 2D vector
 */
export declare function magnitude(x: number, y: number): number;
/**
 * calculates the angle (in radians) of a 2D vector
 */
export declare function angle(x: number, y: number): number;
/**
 * converts radians to degrees, ensuring the result is in the range [0, 360)
 */
export declare function radiansToDegrees(radians: number): number;
/**
 * converts angle in degrees to the nearest cardinal direction
 */
export declare function degreesToCardinalDirection(angleDegrees: number): CardinalDirection;
export type MotionResultWithDiagonals = {
    justMoved: Record<CardinalDirection, boolean>;
    didJustMove: true;
    direction: CardinalDirection;
} | {
    justMoved: Record<CardinalDirection, boolean>;
    didJustMove: false;
    direction: CardinalDirection | Neutral;
};
export type MotionResultWithoutDiagonals = {
    justMoved: Record<Motion, boolean>;
    didJustMove: boolean;
    direction: {
        x: AxisMotionHorizontal;
        y: AxisMotionVertical;
    };
};
export declare function motion(params: {
    rawX: number;
    rawY: number;
    deadzone: number;
    player: Player;
    stick: Side;
    activationThreshold: number;
    deactivationThreshold: number;
    diagonals: true;
}): MotionResultWithDiagonals;
export declare function motion(params: {
    rawX: number;
    rawY: number;
    deadzone: number;
    player: Player;
    stick: Side;
    activationThreshold: number;
    deactivationThreshold: number;
    diagonals: false;
}): MotionResultWithoutDiagonals;
export {};
